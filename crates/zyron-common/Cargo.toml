[package]
name = "zyron-common"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
thiserror.workspace = true
serde.workspace = true
scc.workspace = true

[features]
# Compiles in the wall-clock phase profiler (crate::profile). Off by default so
# production builds carry no instrumentation; enable for profiling builds.
profile = []

[dev-dependencies]
serde_json = "1.0"
zyron-bench-harness = { path = "../zyron-bench-harness" }
